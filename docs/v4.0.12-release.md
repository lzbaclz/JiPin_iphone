# 极拼 4.0.12（构建 16）

首页的证件照卡片移到贴纸试贴卡片上方。证件照作为主要制作功能先出现，贴纸体验紧随其后。

本次只是交换两个现有卡片的位置，保留各自的点击行为、样式和辅助功能标识。没有新增测试代码；复用原有的两个入口 UI 流程进行检查。

两个既有 UI 用例通过：`testV3HomeOpensDecoratedSample` 和 `test01HomeEntryFiveTemplatesAndCustomValidation`。另逐字核对两个卡片的代码块未变，证件照顺序在前。验证记录：Git 忽略的 `build/HomePriority412/UI.xcresult` 与 `ui.log`。

- 业务提交：`ab939cc`。
- 主 App 与相册扩展均为 4.0.12（16），签名归档与 IPA 校验通过。
- IPA：`build/TestFlight/JiPin-4.0.12-16/Export/JiPin.ipa`，9,877,596 字节。
- SHA-256：`5f760d9d0947d68e2abffeb9358cf053c8872c0693526f44f18217278513af25`。
- 2026-09-13 01:47（英国本地时间）上传成功，Xcode 返回 `Upload succeeded`。
- App Store Connect 网页仍需重新登录，尚未核验处理完成、内测可更新状态或配置本构建的公开内测。本轮未修改正式商店审核。
- 证据：`build/HomePriority412/archive.log`、`ipa-verification.json`、`upload.log`。
