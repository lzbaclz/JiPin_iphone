# 极拼 4.0.13（构建 17）— 单张长图

2026-09-18 用户确认继续发布。代码修改完成，5 项核心测试和 8 项 UI 测试通过，发布归档进行中；最终分发结果见文末。

## 问题与修改

原长图排版引擎已能处理单张照片，但 `PhotoLimits` 将长图范围设置为 2–20，导致首页入口、通用模式选择及编辑器删除保护不允许单张。

- 共用范围改为 1–20；首页和编辑器从同一规则显示数量。
- 通用选图的一张照片现在可选长图模式，说明文字同步更新。
- 可由多张删除至一张，仍禁止删至空白项目；原有添加照片、撤销、草稿和导出流程保持。
- 相册快捷扩展改为 1–9 张。单张默认进入长图，不提供不适用的模板选择；零张仍禁用保存。部分照片导入失败时，剩余一张仍需确认，不能静默忽略失败照片。
- App 内帮助和 README 同步数量说明。主 App 与扩展的版本源已改为 4.0.13（17）。

使用：主页点“长图拼接”后只选一张，或通用“选择照片”选一张再选长图，即可继续裁切、编辑、导出和保存草稿。相册分享扩展选择单张时直接进入长图。

## 已完成验证

模拟器：JiPin-ModeEntry-408（iPhone 17 / iOS 26.3），ID `49BE27C4-9AD6-425A-A872-91B6CDA37D44`。

5 项核心测试通过：数量上下限、扩展数量规则、横向和纵向单张实际 JPEG 与分页输出、从两张减为一张后的持久化／恢复／撤销／添加、单张 Live 的原生配对和动态帧差异。

8 项 UI 测试通过：空白快捷扩展禁止保存、单张快捷扩展横向切换和系统分享、三张长图直接进入、通用单张选图进入长图、单张相册保存后关闭导出及草稿重开、模板一张仍拒绝而长图一张可进入、单张且存在导入失败时必须确认、快捷扩展超量及取消回归。

最终两个扩展用例于 2026-09-18 补跑通过。已人工查看单张长图草稿重开截图，照片数量显示“1 张，本模式 1–20 张”，示例 600×1000 原图的高清输出为 1440×2400，保持比例。没有新增真机相册宿主扩展验收，模拟器主 App 承载的快捷扩展界面验证不能代替这一项。

日志与结果：`/Users/liziqing/Programs/myself/jipin_iphone/build/SingleStrip413/`，包括 `Unit.xcresult`、`UI.xcresult`、`unit.log`、`ui.log`、`FinalUI.xcresult`、`final-ui.log`、`UIReview/` 和生成的模拟器照片。

## 发布过程

9 月 17 日因后台登录卡点三次暂停，未上传。9 月 18 日用户明确要求继续；重新打开极拼实际 TestFlight 地址后成功访问构建列表，确认目前最高版本为 4.0.12（16），随后补跑剩余测试并归档 4.0.13（17）。

本次仅发布新 TestFlight 测试版，正式 App Store 版本单独维护。
